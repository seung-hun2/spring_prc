drop table if exists member CASCADE;
create table member
(
    idx   bigint generated by default as identity,
    name varchar(255),
    id varchar(255),
    password varchar(255),
    primary key (idx)
);